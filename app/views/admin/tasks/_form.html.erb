<%= simple_form_for [:admin,@task] do |f| %>
  <hr>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :wage_lower_bound, :label => "设计费下限" %>
<%= f.input :wage_upper_bound, :label => "设计费上限" %>
<%= f.input :contact_email %>

  <%= f.submit "Submit" %>
<% end %>
